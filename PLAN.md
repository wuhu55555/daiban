# 项目计划：代办事项

> 创建时间：2026-09-03
> 当前状态：已完成

## 项目概述
- **目标**：交付一个纯前端（Vue 3 + TypeScript）代办事项应用，支持事项的增删改查、完成勾选、筛选与 localStorage 持久化，打开即用、刷新不丢。
- **用户**：个人用户（单机单浏览器使用，无需账号体系）。
- **预期日活**：个人工具，单人使用（非多用户产品）。
- **核心约束**：纯前端无后端；数据仅存 localStorage；技术栈 Vue 3 + TS + Vite；不引入 UI 库（原生 CSS）；遵循《AGENTS.md 编码宪法》分层与 git 三连契约。

## 阶段划分

### 阶段一：MVP
- **目标**：核心功能端到端走通——事项的增删改查、完成/取消勾选、全部/未完成/已完成筛选、localStorage 持久化，形成可演示可日常使用的完整闭环。
- **模块划分**：
  - **工程骨架**：Vite + Vue3 + TS 初始化，目录分层（src/components｜hooks｜services｜types｜utils），tsconfig 与 Lint 配置
  - **数据层** `services/todoStorage`：localStorage 读写、结构校验、损坏兜底重置（需求 Q5）
  - **业务层** `hooks/useTodos`：状态管理 + CRUD + 排序（未完成在前/已完成在后，组内 createdAt 倒序）+ 筛选 + 统计 + 标题校验（需求 Q1/Q6/Q9）
  - **UI 层**：`TodoStats`（总数/未完成数）、`TodoForm`（新增/编辑表单）、`TodoFilter`（筛选切换）、`TodoItem`（列表项）、`App.vue`（装配）
  - **测试层**：Vitest 单测覆盖 todoStorage 与 useTodos 核心逻辑（排序/筛选/校验/存储）
- **验收标准**：手工走通「新增 → 编辑 → 勾选完成（删除线+沉底）→ 筛选 → 删除（confirm）→ 刷新数据不丢」完整流程；空标题/超 50 字被拦截；localStorage 脏数据不崩溃；核心逻辑单测全部通过。
- **预计耗时**：1~2 天
- **状态**：已完成 ✅

### 阶段二：优化A
- **目标**：补充非功能质量——交互细节、可访问性、响应式与代码质量门禁，使产品达到「给人用的成品」水准。
- **模块划分**：
  - **UX 打磨**：空态提示、键盘操作（Enter 提交 / Esc 取消）、输入与焦点交互细节
  - **可访问性与响应式**：语义化标签、焦点管理、移动端/窄屏布局适配
  - **质量门禁**：按 `CHECKLIST.md` 六层逐条自检（架构/模块/文件/代码/测试/其他），Lint 通过，函数 ≤50 行、文件 ≤300 行核对
  - **文档同步**：`README.md`「怎么跑」落地（安装/启动/功能/目录说明）
- **验收标准**：CHECKLIST 全项通过；Lint 0 错误；桌面与窄屏布局正常；关键交互（空态、键盘）可用；README 可指导他人运行。
- **预计耗时**：1 天
- **状态**：已完成 ✅

### 阶段三：优化B
- **目标**：为交付做准备——回归验证、构建产物核验、文档与 git 收尾，确认产物可被直接使用。
- **模块划分**：
  - **回归验证**：全功能手工回归（含边界与异常场景）；故意改错验证测试真实性（改错必须失败）
  - **构建与交付**：`npm run build` 产物验证（静态资源可运行）、部署/托管方式说明
  - **收尾**：`ACHIEVEMENT.md` 最终交付总结（交付物/未完成项/备注）、PLAN 状态置 ✅、git 最终提交
- **验收标准**：预发布静态产物在本地静态服务可完整运行；交付物清单齐全；git 历史完整（每 Step 独立 commit）；项目状态全部收尾。
- **预计耗时**：0.5~1 天
- **状态**：已完成 ✅

## 里程碑记录
| 日期 | 里程碑 | 备注 |
|------|--------|------|
| 2026-09-03 | 需求对齐完成 | 9 条疑问逐条拍板，全部按默认假设确认 |
| 2026-09-03 | 设计文档完成 | requirements / architecture / database 三份契约已确认 |
| 2026-09-03 | PLAN 草案完成 | 三阶段目标与模块划分，待用户确认 |
| 2026-09-03 | MVP 阶段完成 | 8/8 步，50 单测全绿，端到端流程打通 |
| 2026-09-03 | 优化A 阶段完成 | 6/6 步，UX/a11y/响应式/ESLint/README，63 单测全绿 |
| 2026-09-03 | 优化B 阶段完成 | 3/3 步，回归 + 产物核验 + 收尾，64 单测全绿，项目交付 |
