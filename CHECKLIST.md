# 代码审查清单（CHECKLIST.md）

## 架构层
- [ ] 目录结构是否符合宪法分层（/api, /components, /hooks, /services）？
- [ ] 是否存在跨层调用（如组件直接操作数据库）？
- [ ] 是否存在循环依赖？
- [ ] 所有配置是否集中在 /config 或通过环境变量管理？

## 模块层
- [ ] 每个模块职责是否单一（无跨域逻辑）？
- [ ] 模块间通信是否通过接口/API，无内部状态共享？
- [ ] 依赖方向是否正确（上层依赖下层）？
- [ ] 模块文件数是否≤5个核心文件？

## 文件层
- [ ] 文件是否≤300行？
- [ ] 导入是否规范（无通配符，无冗余）？
- [ ] 文件命名是否与内容匹配，风格统一？

## 代码层
- [ ] 函数是否≤50行？
- [ ] 圈复杂度是否≤10（嵌套≤3层）？
- [ ] 命名是否有业务含义（无缩写）？
- [ ] 安全红线：无SQL拼接、明文密码、日志泄露？
- [ ] 错误处理：所有外部调用是否有try-catch？
- [ ] 日志规范：关键操作有日志，级别正确？
- [ ] 性能陷阱：无循环查询？有分页？有超时？

## 测试层
- [ ] 核心逻辑是否有对应测试文件？
- [ ] 测试是否覆盖正常、边界、异常三类场景？
- [ ] 测试是否有断言（assert/expect），不是只打log？
- [ ] 测试是否独立（可单独运行）？
- [ ] 测试是否真实有效（故意改错代码，测试会失败吗）？

## 其他层
- [ ] README是否有项目说明、启动步骤？
- [ ] 依赖版本是否锁定，无已知漏洞？
- [ ] API文档是否与代码同步？
- [ ] 代码风格是否统一（Lint通过）？

---

## 速记：6 层审查维度

```
[ ] 架构层：目录结构、模块边界、循环依赖、配置集中
[ ] 模块层：单一职责、对外接口、依赖方向、模块大小
[ ] 文件层：文件长度、导入导出、文件命名
[ ] 代码层：函数长度、圈复杂度、命名规范、安全红线、错误处理、日志规范、性能陷阱
[ ] 测试层：测试存在、测试覆盖、测试真实、测试独立、测试执行
[ ] 其他层：README、依赖管理、文档同步、代码风格
```
